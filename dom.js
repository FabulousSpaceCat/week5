// 3) use JS to change the styles of each of the elements in your list (can be as basic as .color). 
// 4) Change the class name of your second item in your list using child (in js).
// 5) Change the text value of your last item in your list using sibling (in js).
// 6) Create a second list with a list inside of it (nested list). Each list should have at least 2 items. Add a button to the bottom of the page (make sure it's not in a list).
// 7) Create an event listener that is listening for the button click. When the event is triggered, have one the elements in the innermost list change it's display to hidden.